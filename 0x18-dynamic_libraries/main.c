#include "main.h"

/**
 * main - Entry point of the program
 *
 * Description:
 * This function serves as the entry point of the program. It calls the _puts
 * function to print a quote by Kevin Mitnick. The quote is enclosed within
 * double quotes and includes a newline character and a tab escape sequence
 * for formatting. After printing the quote, the function returns 0 to
 * indicate successful execution.
 *
 * Return:
 * This function returns an integer value 0, indicating successful execution.
 */
int main(void)
{
	_puts("At end of the day, my goal was to be the best hacker - Kev Mitnick");
	return (0);
}
